apiVersion: apps/v1
kind: Deployment
metadata:
  name: log-output-dep
spec:
  replicas: 1
  selector:
    matchLabels:
      app: log-output
  template:
    metadata:
      labels:
        app: log-output
    spec:
      containers:
        - name: log-output
          image: log-output:1.7
          # The image is imported with `k3d image import`, not pulled from a registry
          imagePullPolicy: IfNotPresent
          env:
            - name: PORT
              value: "3000"
