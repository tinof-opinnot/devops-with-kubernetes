apiVersion: v1
kind: Service
metadata:
  name: todo-app-svc
spec:
  type: NodePort
  selector:
    app: todo-app
  ports:
    - name: http
      # k3d maps localhost:8082 to port 30080 of agent-0
      nodePort: 30080
      protocol: TCP
      port: 1234
      targetPort: 3000
